"use strict";

const { Board } = require("./board.model");
const { Thread } = require("./thread.model");
const { Reply } = require("./reply.model");

module.exports = {
  Board,
  Thread,
  Reply,
};
